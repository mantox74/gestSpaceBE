info:
  name: Clienti Ricerca
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/api/clienti
  body:
    type: json
    data: |-
      {
        "search": "a",
        "page": 1,
        "limit": 10
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
