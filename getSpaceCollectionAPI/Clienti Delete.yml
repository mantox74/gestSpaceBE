info:
  name: Clienti Delete
  type: http
  seq: 11

http:
  method: DELETE
  url: http://localhost:3000/api/clienti/2
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
