info:
  name: Spazio Crea
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:3000/api/spazi/nuovo
  body:
    type: json
    data: |-
      {
        "nome": "Container C",
        "descrizione": "Container 30 piedi",
        "lunghezza": 10.00,
        "larghezza": 3.40,
        "altezza": 4.60,
        "prezzo_giorno": 35.00,
        "note": "Ingresso lato sud/est"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
