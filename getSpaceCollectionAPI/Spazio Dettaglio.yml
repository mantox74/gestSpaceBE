info:
  name: Spazio Dettaglio
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:3000/api/spazi/{{spazio_id}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
