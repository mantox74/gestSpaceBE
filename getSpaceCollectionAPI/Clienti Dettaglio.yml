info:
  name: Clienti Dettaglio
  type: http
  seq: 12

http:
  method: GET
  url: http://localhost:3000/api/clienti/1
  body:
    type: json
    data: |-
      {
        "search": "a",
        "page": 1,
        "limit": 10
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
