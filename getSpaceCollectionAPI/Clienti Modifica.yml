info:
  name: Clienti Modifica
  type: http
  seq: 10

http:
  method: PUT
  url: http://localhost:3000/api/clienti/1
  body:
    type: json
    data: |-
      {
        "indirizzo": "Via Roma",
      "numero_civico": 15,
        "citta": "Bollate",
        "provincia": "MI"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
